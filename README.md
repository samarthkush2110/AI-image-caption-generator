# AI-image-caption-generator
AI Image Caption Generator is a deep learning–based system that generates meaningful captions from images. It uses MobileNetV2 for image classification and ViT-GPT2 for caption generation. Built on a custom dataset, it provides fast, accurate, and user-friendly results via a Gradio interface.
